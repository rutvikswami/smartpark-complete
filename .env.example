# SmartPark Unified Configuration Example
# Copy this file to .env and fill in your actual values

# ==========================================
# FRONTEND (React/Vite) CONFIGURATION
# ==========================================
# Google Maps API Key - Get from: https://console.cloud.google.com/google/maps-apis
# Required for: Map display, location services
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Supabase Database Configuration (Frontend)
# Get from: https://app.supabase.com/project/[your-project]/settings/api
# Required for: User authentication, real-time data updates, database operations
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ==========================================
# BACKEND (Python/Monitor) CONFIGURATION
# ==========================================
# Supabase Database Configuration (Backend)
# Same as above but without VITE_ prefix for Python scripts
# Required for: Parking slot monitoring, status updates, system health
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# User Authentication Credentials
# Account for system authentication and database access
# Required for: Monitoring system authentication, location access
USER_EMAIL=your_email@domain.com
USER_PASSWORD=your_secure_password

# ==========================================
# MONITORING SYSTEM CONFIGURATION
# ==========================================
# Location Selection: System will fetch available locations from database
# Available locations from your database:
# - Phoenix MarketCity (password: phoenix123)
# - CMR Institute of Technology (password: cmrit123)  
# - Forum Mall Marathahalli (password: forum123)
# - Brookefield Mall (password: brook123)
# - Marathahalli Bridge Metro Station (password: metro123)
# - Whitefield Main Road (password: white123)

# YOLO Model Configuration
# Model file for vehicle detection (yolov8n.pt is lightweight, 6MB)
# Download automatically on first run or get from: https://github.com/ultralytics/ultralytics
YOLO_MODEL=yolov8n.pt

# Detection confidence threshold (0.0-1.0)
# Lower = more detections but may include false positives
# Higher = fewer but more accurate detections
DETECTION_CONFIDENCE_THRESHOLD=0.3

# Vehicle class IDs for COCO dataset
# 2=car, 5=bus, 7=truck - comma separated
VEHICLE_CLASSES=2,5,7

# Monitoring Settings
# Number of frames to skip for performance (higher = faster but less accurate)
FRAME_SKIP_COUNT=3

# How often to send heartbeat to database (seconds)
HEARTBEAT_INTERVAL=30

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================
# 1. Copy this file to .env in the smartpark root directory
# 2. Create a Supabase project at https://app.supabase.com
# 3. Get a Google Maps API key from Google Cloud Console
# 4. Create user accounts in your Supabase auth system
# 5. Update all placeholder values with your actual credentials
# 6. For React app: ensure variables start with VITE_
# 7. For Python scripts: use variables without VITE_ prefix
# 8. Monitoring system will prompt to select location from available options
#
# Security Note: Never commit .env files to version control
# Add .env to your .gitignore file to keep secrets safe